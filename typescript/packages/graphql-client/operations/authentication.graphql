mutation signIn($signInCredentials: SignInCredentials!) {
  signIn(signInCredentials: $signInCredentials) {
    username
    email
    age
  }
}


mutation signUp($user: UserInput!) {
  signUp(user: $user) {
    username
    email
    age
  }
}


mutation signOut {
  signOut {
    userId
    message
  }
}


query session {
  session {
    user {
      name
      email
      image
    }
    expiresIn
  }
}

mutation createOrUpdateUserOauth(
  $account: AccountOauthInput!
  $profile: ProfileOauth!
) {
  createOrUpdateUserOauth(account: $account, profile: $profile) {
    username
    email
    age
  }
}
